import { SplitPipe } from './split-pipe';

describe('SplitPipe', () => {
  it('should create an instance', () => {
    const directive = new SplitPipe();
    expect(directive).toBeTruthy();
  });
});
